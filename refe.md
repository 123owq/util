직접 확인했습니다. 이 PDF 구조 분석 결과:

---

## 구조 분석 결과

**1~5페이지 → 텍스트 기반 ✅**
- PyMuPDF로 100% 정확하게 추출 가능
- 본문, 거절이유, 인용발명 내용 전부 OK

**2페이지 표 (청구항 vs 인용발명 1 비교표) → 네이티브 PDF 표 ✅**
- `pdfplumber`의 `extract_table()`로 셀 단위 추출 가능
- 다만 셀 안에 줄바꿈이 많아서 후처리로 공백 정리 필요

**6페이지(마지막 안내 페이지) → ⚠️ 이미지 삽입**
- 마지막 페이지는 이미지로 렌더링된 페이지임
- 텍스트 추출하면 빈 페이지로 나옴
- 안내문 내용이 필요하면 OCR 필요, 필요 없으면 그냥 스킵하면 됨

---

## 실용적인 결론

| 목적 | 라이브러리 | 정확도 |
|---|---|---|
| 본문 전체 텍스트 | PyMuPDF | 100% |
| 2페이지 비교표 | pdfplumber | 95%+ (셀 정리 필요) |
| 6페이지 안내문 | EasyOCR 등 | 필요시만 |

---

**배치 자동화 추천 흐름:**
1. PyMuPDF로 전체 텍스트 추출 → txt 저장
2. pdfplumber로 표 감지 → 표 있으면 별도 CSV 저장
3. 페이지별 텍스트가 비어있으면 → 이미지 페이지로 로그 기록

일단은 그 볼드체냐 밑줄처진 그게 있잖아 소제목같은 애들.

걔들이랑 하위 단락내용을 매치로 json 형식으로 저장하는건 어때 

나중에 더 파싱을 할건데 이 방법처럼만 해두면 일단 정보손실은, 변환은  1도없는거아니냐

오후 3:38

헤더-콘텐츠 매핑 구조로 정보손실 없이 JSON 변환 가능 확인했음

정보손실 0% 맞아요. 이유는:

모든 텍스트를 다 저장하되, 구조만 추가하는 거니까
나중에 JSON에서 꺼내 쓰면 원본과 동일
PyMuPDF로 가능한가?
가능합니다. page.get_text("dict") 로 추출하면 문자/단어 단위로 아래 정보가 다 딸려옴:

bold 여부 (flags 값으로 판단)
size (폰트 크기)
font 이름
밑줄은 별도 플래그 or 좌표로 감지
예상 JSON 구조
json
{
  "sections": [
    {
      "header": "[심사결과]",
      "content": "심사 대상 청구항 : 제1-10항 ..."
    },
    {
      "header": "1-1. 청구항 제1항에 대한 용이발명 여부",
      "content": "청구항 제1항의 시트고무 가공성 ..."
    }
  ]
}
한 가지 확인할 것
이 PDF에서 소제목 구분 기준이 볼드체인지, 폰트 크기인지, 밑줄인지 실제로 플래그 찍어봐야 확실함. 눈으로 볼드처럼 보여도 실제로는 폰트명이 다른 경우 있음 (예: KoreanBold vs Korean).

코드 짜드릴까요? 실제 이 PDF 돌려서 어떤 플래그로 소제목이 구분되는지 먼저 확인하고, 그 기준으로 파싱 로직 짜는 게 순서예요.

